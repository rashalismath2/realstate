import { createMuiTheme, colors } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#009432"
    },
    secondary: {
      main: "#ffed4a"
    },
    text: {
      primary: "#000",
      secondary:"#fff"
    }
  }
});

export default theme;